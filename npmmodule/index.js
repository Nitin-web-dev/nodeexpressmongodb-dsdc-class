// use validator module to use for validation in inputs or values   
// const validator = require('validator');
import validator from "validator";


// isEmail check if email is valid or not ;
console.log(validator.isEmail('sam@gmail.com'));
// chaeck by validator.  it will all values ;
// console.log(validator.)

